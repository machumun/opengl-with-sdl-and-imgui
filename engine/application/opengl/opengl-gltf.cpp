#include "opengl-gltf.hpp"

using hid::OpenGLGLTF;

void OpenGLGLTF::draw(const hid::OpenGLShader &shader) const
{
    // GLuint maxInt = meshes.size();
    // for (int i = 0; i < maxInt; ++i)
    // {
    //     shader.setMat4("u_modelMatrix", &gltf.matricesMeshes[i][0][0]);
    //     // textures[i].bind();
    //     meshes[i].draw();
    // }
}