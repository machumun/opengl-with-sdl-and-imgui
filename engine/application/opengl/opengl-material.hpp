#include "../../core/asset-inventory.hpp"

namespace hid
{
    struct OpenGLMaterial
    {
        hid::assets::Texture albedoMap;
        hid::assets::Texture metaricMap;
        hid::assets::Texture roughnessMap;
        hid::assets::Texture metaricMap;
        hid::assets::Texture normalMap;
        hid::assets::Texture emissive;
    };
}