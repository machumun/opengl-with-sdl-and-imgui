#pragma once

// #include ""

namespace hid
{
    struct FileManager
    {
        FileManager() = default;
        ~FileManager() = default;

        void saveScene()
        {
        }

        void loadScene()
        {
        }

        void importAsset() {}

        void buildGame() {}
    };
}