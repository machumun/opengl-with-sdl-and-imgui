#pragma once

namespace hid
{
    struct FileManager
    {
        FileManager() = default;
        ~FileManager() = default;

        void saveScene()
        {
        }

        void loadScene()
        {
        }
    };
}